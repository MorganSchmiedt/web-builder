# Node.JS Website Builder

## Usage

```javascript
const buildiler = require('@deskeen/web-builder')

await builder.build({
  source: [
    // List of files or directories
  ],
  modules: [
    // List of modules
  ]
})
```